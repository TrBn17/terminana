[
  {
    "name": "calculate",
    "description": "Safely evaluate a basic arithmetic expression.",
    "module": "terminana.core.system_tools",
    "function": "evaluate_expression",
    "parameters": {
      "type": "object",
      "properties": {
        "expression": {
          "type": "string",
          "description": "A mathematical expression to evaluate, e.g. '2 ** 10 + 5 * 3'."
        }
      },
      "required": [
        "expression"
      ]
    }
  },
  {
    "name": "get_system_info",
    "description": "Get current system information for the requested section.",
    "module": "terminana.core.system_tools",
    "function": "get_system_info",
    "parameters": {
      "type": "object",
      "properties": {
        "sections": {
          "type": "string",
          "description": "Which info sections to include: 'all', 'os', 'python', 'time'. Default 'all'.",
          "default": "all"
        }
      }
    }
  },
  {
    "name": "run_command",
    "description": "Run a shell command and return the captured output.",
    "module": "terminana.core.pexpect_tools",
    "function": "run_command",
    "parameters": {
      "type": "object",
      "properties": {
        "command": {
          "type": "string",
          "description": "The shell command to execute, e.g. 'dir' or 'python --version'."
        },
        "timeout": {
          "type": "integer",
          "description": "Maximum seconds to wait for the command to finish. Default 30.",
          "default": 30
        }
      },
      "required": [
        "command"
      ]
    }
  },
  {
    "name": "spawn_and_interact",
    "description": "Spawn a process and execute ordered expect/send interaction steps.",
    "module": "terminana.core.pexpect_tools",
    "function": "spawn_and_interact",
    "parameters": {
      "type": "object",
      "properties": {
        "command": {
          "type": "string",
          "description": "The executable or command to spawn."
        },
        "interactions": {
          "type": "array",
          "description": "Ordered list of interaction steps. Each step has optional 'expect' (pattern to wait for) and 'send' (text to type after match).",
          "items": {
            "type": "object"
          }
        },
        "timeout": {
          "type": "integer",
          "description": "Per-step timeout in seconds. Default 30.",
          "default": 30
        }
      },
      "required": [
        "command",
        "interactions"
      ]
    }
  }
]
